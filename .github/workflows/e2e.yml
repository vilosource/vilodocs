name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Install Playwright dependencies
        run: |
          npx playwright install-deps
          npx playwright install
      
      - name: Build application
        run: |
          # Start electron-forge with a timeout to trigger the build process
          # This will build the .vite/build/ directory then we kill it
          timeout 30 npx electron-forge start --inspect-electron || true
          echo "Build complete, checking for main.js..."
          ls -la .vite/build/ || echo "Build directory not found"
      
      - name: Run E2E tests
        run: xvfb-run -a npm run test:e2e
        env:
          CI: true
      
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          retention-days: 7
      
      - name: Upload test screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: test-results
          retention-days: 7